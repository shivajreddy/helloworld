# Watch Go files and restart the Go server
-r '\.go$' -s -- go run cmd/server/main.go

# Watch SQL migration files and run them
-r '\.sql$' -s -- go run cmd/migrate/main.go

# Watch React files and rebuild frontend
-r '(\.jsx?|\.tsx?|\.css)$' -- sh -c 'cd frontend && npm run build'
